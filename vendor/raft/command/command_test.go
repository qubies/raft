package command

import "testing"

func TestMain(t *testing.T) {
	/*write := new(bytes.Buffer)
	me := member.New(member.GetIP()+":44444", "localhost", "commanderTest", write)
	stop := make(chan bool)
	go Begin(me, write, stop)
	time.Sleep(time.Second)*/
}
